---
title: 【Mac】ショートカット集（VSCode編）
date: '2024-09-2'
tags: ['ショートカット']
draft: false
summary: VSCodeのショートカット集
---

## 基本

<div className="w-full overflow-x-auto">
  <table style={{ margin: '0 0' }}>
    <thead>
      <tr>
        <th style={{ textAlign: 'left', width: '50%' }}>キー</th>
        <th style={{ textAlign: 'left', width: '50%' }}>説明</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td style={{ textAlign: 'left' }}>**「⌘（Command）」+「X」**</td>
        <td style={{ textAlign: 'left' }}>選択範囲を切り取り／行を切り取り（空選択時）</td>
      </tr>
      <tr>
        <td style={{ textAlign: 'left' }}>**「⌘（Command）」+「C」**</td>
        <td style={{ textAlign: 'left' }}>選択範囲をコピー／行をコピー（空選択時）</td>
      </tr>
      <tr>
        <td style={{ textAlign: 'left' }}>**「⌘（Command）」+「V」**</td>
        <td style={{ textAlign: 'left' }}>貼り付け</td>
      </tr>
      <tr>
        <td style={{ textAlign: 'left' }}>**「⇧（Shift）」+「⌘（Command）」+「K」**</td>
        <td style={{ textAlign: 'left' }}>行を削除</td>
      </tr>
      <tr>
        <td style={{ textAlign: 'left' }}>**「⌘（Command）」+「Enter」**</td>
        <td style={{ textAlign: 'left' }}>下に行を挿入</td>
      </tr>
      <tr>
        <td style={{ textAlign: 'left' }}>**「⇧（Shift）」+「⌘（Command）」+「Enter」**</td>
        <td style={{ textAlign: 'left' }}>上に行を挿入</td>
      </tr>
      <tr>
        <td style={{ textAlign: 'left' }}>**「⌥（Option）」+「↑」**</td>
        <td style={{ textAlign: 'left' }}>選択範囲行を上に移動する／行を上に移動する（未選択時）</td>
      </tr>
      <tr>
        <td style={{ textAlign: 'left' }}>**「⌥（Option）」+「↓」**</td>
        <td style={{ textAlign: 'left' }}>選択範囲行を下に移動する／行を下に移動する（未選択時）</td>
      </tr>
      <tr>
        <td style={{ textAlign: 'left' }}>**「⇧（Shift）」+「⌥（Option）」+「↑」**</td>
        <td style={{ textAlign: 'left' }}>選択範囲行を上にコピーする／行を上にコピーする（未選択時）</td>
      </tr>
      <tr>
        <td style={{ textAlign: 'left' }}>**「⇧（Shift）」+「⌥（Option）」+「↓」**</td>
        <td style={{ textAlign: 'left' }}>選択範囲行を下にコピーする／行を下にコピーする（未選択時）</td>
      </tr>
      <tr>
        <td style={{ textAlign: 'left' }}>**「⌘（Command）」+「Z」**</td>
        <td style={{ textAlign: 'left' }}>直前の操作を元に戻す</td>
      </tr>
      <tr>
        <td style={{ textAlign: 'left' }}>**「⇧（Shift）」+「⌘（Command）」+「Z」**</td>
        <td style={{ textAlign: 'left' }}>取り消した操作をやり直す</td>
      </tr>
      <tr>
        <td style={{ textAlign: 'left' }}>**「⌘（Command）」+「D」**</td>
        <td style={{ textAlign: 'left' }}>次の一致する単語を選択する</td>
      </tr>
      <tr>
        <td style={{ textAlign: 'left' }}>**「⌘（Command）」+「U」**</td>
        <td style={{ textAlign: 'left' }}>最後のカーソル操作を元に戻す</td>
      </tr>
      <tr>
        <td style={{ textAlign: 'left' }}>**「⇧（Shift）」+「⌥（Option）」+「I」**</td>
        <td style={{ textAlign: 'left' }}>選択範囲行の末尾にカーソルを追加する</td>
      </tr>
      <tr>
        <td style={{ textAlign: 'left' }}>**「⌘（Command）」+「F2」**</td>
        <td style={{ textAlign: 'left' }}>現在選択している単語を全て選択する</td>
      </tr>
    </tbody>
  </table>
</div>

## 参考

- [Key Bindings for Visual Studio Code](https://code.visualstudio.com/docs/getstarted/keybindings)
